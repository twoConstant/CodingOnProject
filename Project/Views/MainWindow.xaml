<Window x:Class="Project.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:local="clr-namespace:Project"
        Title="Sensor Dashboard" Height="600" Width="900">

    <Grid>
        <TabControl RenderTransformOrigin="0.783,0.369">
            <!-- Tab 1: Machine Information -->
            <TabItem Header="Machine Information">
                <Grid Margin="10">
                    <StackPanel>
                        <!-- Title -->
                        <TextBlock Text="Machine Information" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" />

                        <!-- Machine ID 입력 -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="Machine ID:" VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBox x:Name="MachineIdInput" Width="200" />
                            <Button Content="Load" Margin="10,0,0,0" Click="OnLoadButtonClick" />
                        </StackPanel>

                        <!-- Machine 데이터 표시 -->
                        <TextBlock Text="Machine ID:" />
                        <TextBlock Text="{Binding Machine.MachineId}" />

                        <TextBlock Text="Device ID:" />
                        <TextBlock Text="{Binding Machine.DeviceId}" />

                        <TextBlock Text="Manufacturer:" />
                        <TextBlock Text="{Binding Machine.DeviceManufacturer}" />

                        <TextBlock Text="Device Name:" />
                        <TextBlock Text="{Binding Machine.DeviceName}" />

                        <TextBlock Text="Type:" />
                        <TextBlock Text="{Binding Machine.Type}" />
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Tab 2: Sensor Dashboard -->
            <TabItem Header="Sensor Dashboard">
                <Grid>
                    <StackPanel Orientation="Vertical">
                        <!-- Header -->
                        <TextBlock Text="Machine ID:" />
                        <TextBlock Text="{Binding Machine.MachineId}" FontSize="10" Margin="10,0,0,0" />
                        <TextBlock Text="Device ID:" />
                        <TextBlock Text="{Binding Machine.DeviceId}" FontSize="10" Margin="10,0,0,0" />
                        <TextBlock Text="Manufacturer:" />
                        <TextBlock Text="{Binding Machine.DeviceManufacturer}" FontSize="10" Margin="10,0,0,0" />
                        <TextBlock Text="Device Name:" />
                        <TextBlock Text="{Binding Machine.DeviceName}" FontSize="10" Margin="10,0,0,0" />
                        <TextBlock Text="가동상태: ON" FontSize="10" Margin="10,0,0,0" />

                        <!-- Charts Section -->
                        <StackPanel Orientation="Horizontal" Margin="20" Height="250">
                            <!-- NTC Graph -->
                            <Border BorderBrush="Gray" BorderThickness="1" Margin="10">
                                <lvc:CartesianChart Series="{Binding SensorDataViewModel.NtcValues}" Width="250">
                                    <lvc:CartesianChart.AxisX>
                                        <lvc:Axis Title="Time" Labels="{Binding SensorDataViewModel.TimeLabels}" />
                                    </lvc:CartesianChart.AxisX>
                                </lvc:CartesianChart>
                            </Border>
                            <!-- PM Graphs -->
                            <Border BorderBrush="Gray" BorderThickness="1" Margin="10">
                                <lvc:CartesianChart Series="{Binding SensorDataViewModel.PmValues}" Width="250">
                                    <lvc:CartesianChart.AxisX>
                                        <lvc:Axis Title="Time" Labels="{Binding SensorDataViewModel.TimeLabels}" />
                                    </lvc:CartesianChart.AxisX>
                                </lvc:CartesianChart>
                            </Border>

                            <!-- CT Graphs -->
                            <Border BorderBrush="Gray" BorderThickness="1" Margin="10">
                                <lvc:CartesianChart Series="{Binding SensorDataViewModel.CtValues}" Width="250">
                                    <lvc:CartesianChart.AxisX>
                                        <lvc:Axis Title="Time" Labels="{Binding SensorDataViewModel.TimeLabels}" />
                                    </lvc:CartesianChart.AxisX>
                                </lvc:CartesianChart>
                            </Border>

                        </StackPanel>
                        <!-- Load Data Button -->
                        <Button Content="Load Data" Command="{Binding LoadDataCommand}" CommandParameter="1" Margin="10" />
                    </StackPanel>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</Window>
